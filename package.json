{
  "name": "pace",
  "version": "0.1.0",
  "description": "A fuel gauge for your AI subscription — one glance tells you if you're in the zone.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "license": "MIT",
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1"
  }
}
